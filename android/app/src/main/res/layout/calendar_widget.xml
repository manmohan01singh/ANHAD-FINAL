<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/calendar_widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/widget_background"
    android:gravity="center">

    <!-- Date Header -->
    <TextView
        android:id="@+id/calendar_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monday, Jan 1"
        android:textSize="13sp"
        android:textColor="#B3FFFFFF"
        android:gravity="center" />

    <!-- Countdown Section -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="8dp">

        <TextView
            android:id="@+id/calendar_countdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--"
            android:textSize="36sp"
            android:textStyle="bold"
            android:textColor="#FF9500" />

        <TextView
            android:id="@+id/calendar_countdown_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="days"
            android:textSize="14sp"
            android:textColor="#B3FFFFFF"
            android:layout_marginStart="4dp" />
    </LinearLayout>

    <!-- Event Name -->
    <TextView
        android:id="@+id/calendar_event_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="until next Gurpurab"
        android:textSize="14sp"
        android:textColor="#FFFFFF"
        android:layout_marginTop="2dp"
        android:gravity="center" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#33FFFFFF"
        android:layout_marginVertical="10dp" />

    <!-- Hukamnama Preview -->
    <TextView
        android:id="@+id/calendar_hukamnama"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="☬ Tap to read today's Hukamnama"
        android:textSize="12sp"
        android:textColor="#B3FFFFFF"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>
